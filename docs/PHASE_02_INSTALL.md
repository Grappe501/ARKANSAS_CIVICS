
Phase 02 — Identity, Authentication, Permissions

Install steps:

1. Extract engine files into /engine
2. Extract scripts into /scripts
3. Extract config into /config

Then run:

python scripts/build_phase_02_identity.py

This phase introduces:
- Identity Manager
- Role Permission Engine
- Token Authentication layer
