# Watch Mode (future)

Recommended next script:
- use `watchdog` to monitor `content/` and rerun builds automatically whenever a segment changes.

Suggested future command:
```bash
python scripts/watch_build.py
```
